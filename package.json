{
  "name": "incubyte",
  "type": "module",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "start": "bun --filter '*' start",
    "lint": "bun --filter '*' lint",
    "test": "bun --filter '*' test",
    "seed": "bun --filter @incubyte/api seed"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
